name: Lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
      - uses: cclauss/GitHub-Action-for-pylint@0.7.0
      - run: pip install -r requirements.txt && pylint --recursive=y .

